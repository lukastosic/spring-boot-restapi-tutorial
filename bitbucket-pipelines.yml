pipelines:
  branches:
    master:
      - step:
          image: maven:3.3.9
          script:
            - mvn clean package -Dspring.profiles.active=ci
          services:
            - database
definitions:
  services:
    database:
      image: mariadb
      environment:
        MYSQL_ROOT_PASSWORD: root       